[toolchain]
anchor_version = "0.30.1"

[features]
resolution = true
skip-lint = false

[programs.devnet]
act_presale = "5hmgnujNib14NDkEgRsLpY3H8SBDCsLryiymNKY6fWku"

[programs.mainnet]
act_presale = "5hmgnujNib14NDkEgRsLpY3H8SBDCsLryiymNKY6fWku"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

# Program id generated via `solana-keygen new` on this machine, 2026 (see
# target/deploy/act_presale-keypair.json). Real keypair capable of signing a
# deploy transaction -- gitignored, never committed. Not yet used to deploy
# anywhere. Devnet only until explicit approval to go further (see
# PRESALE_DESIGN.md).
