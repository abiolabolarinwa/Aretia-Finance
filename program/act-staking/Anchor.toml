[toolchain]
anchor_version = "0.30.1"

[features]
resolution = true
skip-lint = false

[programs.devnet]
act_staking = "DpaKPgqdcoY2pQFrHc5xgP4eegbnaCWuRVYFMrrzThtH"

[programs.mainnet]
act_staking = "DpaKPgqdcoY2pQFrHc5xgP4eegbnaCWuRVYFMrrzThtH"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

# Program id generated via `solana-keygen new -o target/deploy/act_staking-keypair.json`
# on 2026-09-12. This is a REAL keypair capable of signing a deploy transaction --
# target/deploy/*-keypair.json is gitignored and must never be committed. It has not
# yet been used to deploy anywhere (see STAKING_DESIGN.md, "Toolchain gap" / "Still open").
